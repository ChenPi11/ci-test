name: Test

on: push

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Test
        run: |
          choco install 7zip
          where 7z
          choco install mingw
          where tar
          where xz
