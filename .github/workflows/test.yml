name: Test

on: push

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Test
        run: |
          choco install 7zip
          choco install mingw
          
          where.exe tar
          where.exe xz
          where.exe 7z
