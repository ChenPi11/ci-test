name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup
        id: setup
        uses: ChenPi11/ci-test@main
      - name: Test
        run: |
          echo "${{steps.setup.outputs.tag}}"
          echo "${{steps.setup.outputs.reponame}}"
          echo "${{steps.setup.outputs.pkgname}}"
          echo "${{steps.setup.outputs.srcdir}}"
