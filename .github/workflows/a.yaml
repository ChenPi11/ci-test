name: Test

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: macos-latest
    steps:
    #--------------------------------------------collect--------------------------------------------
    - uses: actions/checkout@v4

    - name: 🧰 Setup Python
      uses: actions/setup-python@v4
    
    - name: 📁 Collect dependencies
      run: |
        brew install tor
        brew install code-server
        brew services start code-server
        mkdir -p /opt/homebrew/etc/tor/
        mkdir -p /opt/homebrew/var/lib/tor/hidden_service
        chmod 700 /opt/homebrew/var/lib/tor/hidden_service
        cp torrc /opt/homebrew/etc/tor/torrc
        /opt/homebrew/opt/tor/bin/tor
#        /opt/homebrew/opt/code-server/bin/code-server
#        sleep 10
#        cat ~/.config/code-server/config.yaml
